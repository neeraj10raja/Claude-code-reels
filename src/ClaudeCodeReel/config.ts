// ─── EDIT THIS FILE FOR EACH NEW POST ────────────────────────────────────────
//
// Workflow:
//   1. Fill in the fields below for your feature
//   2. python3 scripts/generate_voice.py --feature <name> --ref public/your-voice.m4a
//   3. npm run render -- --output out/<name>-reel.mp4
//
// ─────────────────────────────────────────────────────────────────────────────

export const FEATURE = {
  // Badge shown at top of title card
  badge: "Claude Code · New Feature",

  // Feature name — shown large on title card
  title: "Hooks",

  // One-liner subtitle — \n for line break
  subtitle: "Auto-run shell commands\non every Claude action",

  // Audio file in public/ — generated by scripts/generate_voice.py
  audioFile: "hooks-voice.m4a",

  // ── Scene 2: What it does ─────────────────────────────────────────────────
  bullets: [
    { icon: "⚡", text: "Fire before/after any tool use" },
    { icon: "🎨", text: "Auto-format on every file edit" },
    { icon: "🛡️", text: "Block dangerous commands" },
    { icon: "🔗", text: "Pipe Claude into your workflow" },
  ],

  // ── Scene 3: Terminal demo ────────────────────────────────────────────────
  terminal: {
    userMessage: "Edit app.ts and add error handling",
    steps: [
      {
        tool: "Read",
        input: "src/app.ts",
        output: "142 lines captured",
        color: "#2997FF",
        delay: 55,
        duration: 28,
        isHook: false,
      },
      {
        tool: "Edit",
        input: "src/app.ts",
        output: "Added try/catch blocks",
        color: "#2997FF",
        delay: 95,
        duration: 30,
        isHook: false,
      },
      {
        tool: "↳ HOOK",
        input: "PostToolUse · prettier src/app.ts",
        output: "✓ Formatted · 180ms",
        color: "#34d399",
        delay: 138,
        duration: 22,
        isHook: true,
      },
      {
        tool: "Edit",
        input: "src/utils.ts",
        output: "Error propagation added",
        color: "#2997FF",
        delay: 175,
        duration: 30,
        isHook: false,
      },
      {
        tool: "↳ HOOK",
        input: "PostToolUse · prettier src/utils.ts",
        output: "✓ Formatted · 140ms",
        color: "#34d399",
        delay: 218,
        duration: 22,
        isHook: true,
      },
    ],
    summary: "✓ Edited + auto-formatted. Zero manual steps.",
    summaryFrame: 252,
  },

  // ── Scene 4: Before / After ───────────────────────────────────────────────
  impact: {
    before: { value: "Manual", detail: "Run formatter after every edit" },
    after: { value: "Auto", detail: "Hooks fire automatically" },
    metric: "0",
    metricSuffix: " manual steps",
    metricLabel: "per session",
  },

  // ── Scene 5: CTA ──────────────────────────────────────────────────────────
  cta: {
    handle: "@your.handle",
    tagline: "New Claude Code feature every day",
    followText: "Follow for more",
  },
} as const;
